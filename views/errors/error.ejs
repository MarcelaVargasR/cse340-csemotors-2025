<%# A consistant header among pages%>
 <% if (title) { %>
    <h1><%= title %></h1>
  <% } else { %>
    <h1>Oops!</h1>
  <% } %>

  <% if (message) { %>
    <p><%= message %></p>
  <% } else { %>
    <p> Sorry, we appear to have lost that page..</p>
  <% } %>